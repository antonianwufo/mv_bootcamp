const array = ['apples','pears','bananas'];

const arrayLength = array.length;
console.log('array length is ' + arrayLength);

const firstElement = array[0]; // arrays start at index 0
console.log('first element is ' + firstElement);

const secondElement = array[1]; // arrays start at index 0
console.log('second element is ' + secondElement);

const thirdElement = array[2]; // arrays start at index 0
console.log('third element is ' + thirdElement);

console.log(array.reverse)